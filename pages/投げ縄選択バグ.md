
from [[pRegroup-done-2019]]
投げ縄選択バグ
    - 点線はクリックや新規ドラッグ開始で消えるが、選択された付箋のハイライト状態は消えない
    - グループが選択されてない？
- 後者は単なる実装もれ
- 前者は選択状態をハイライトする実装がLassoTool初期化時のselectionを掴んでしまっていたので、ハイライトを消すコードが初期状態のselection (`= []`)に対して走っているのでハイライト解除されないというバグ
