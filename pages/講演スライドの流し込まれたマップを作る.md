
from [[pRegroup2020]]
講演スライドの流し込まれたマップを作る
- PDFインポート
    - [[画像付箋]]
    - 講演スライドを100枚流し込んだマップを作る(アプリ内データで)
        - 専用のデータをアプリ内に作るのではなく、複数行テキストをまとめてインポートする機能をつけた
            - [[複数行テキストを個別の付箋としてインポート]]
    - 現実的な速度で動くかどうか
        - PC上で移動の度の再描画が点滅に見える
            - レンダリングが終わってから表示されるべきなので点滅するのはおかしい
                - RasterがonLoadで描かれるので非同期なのだ
            - これはpositionをonLoadの外で設定することで解消した
            - 付箋のサイズに合わない画像をロードした時には大きいのが書かれてから小さくなるんだろうな
    - iPadで実験
    - デモ用画面にするかどうか検討
        - とりあえず作ってみて、デモに向いてなさそうだったら別URLにしておけば良い
    - PDFをUploadする機能を作るのではなく、まずはすでにサーバに上がっている画像のリストを使う
