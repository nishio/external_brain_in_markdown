
- 表札の編集UIをグループ作成時に開く

現在の「末尾のitemが編集対象」だとグループの中の表札を編集できないので
「編集ターゲット」を持つ必要があるのだろうけど
例えば編集中にグループを移動したりするとグループの子要素である表札オブジェクトも更新されてしまうので
古いオブジェクトへの参照を持ってても編集が続行できないし、そもそも古い参照を持っているのはバグの元

移動したタイミングで編集ウィンドウは閉じるべきだし、閉じたタイミングで参照はnullになるべき

[[pRegroup-done-2019]]
